# 同频 · 通用课堂反馈 MVP

[English](README.en.md)

项目仓库：[bd4rex/tongpin-classroom-feedback](https://github.com/bd4rex/tongpin-classroom-feedback)。仓库已公开，无需登录 GitHub 即可浏览或克隆。

当前本地版本：`0.4.0`（尚未推送），内置《无处不在的人工智能》40 分钟整课，新增填空题和教师讲解提示。保留统一工作台、快速备课、任务包与按题导出。

一个老师、一节当前课堂、很多学生现场加入。适用于数学、语文、英语、科学、人工智能等课程。音视频继续使用原有会议系统。

学生没有账号，也不需要提前导入名单。输入课堂码后，默认填写姓名或昵称、江苏省内城市、学校和年级／班级；教师可以在后台调整显示和必填项。当前学生入口按个人参与设计，小组和整班模式暂不在加入页展示。

## 文档导航

| 文档           | 中文                                            | 英文                                                  |
| -------------- | ----------------------------------------------- | ----------------------------------------------------- |
| 项目说明与部署 | [README.md](README.md)                          | [README.en.md](README.en.md)                          |
| 验证记录       | [TEST_REPORT.md](TEST_REPORT.md)                | [TEST_REPORT.en.md](TEST_REPORT.en.md)                |
| 更新与交接日志 | [TIMESTAMP_LOG.md](TIMESTAMP_LOG.md)            | [TIMESTAMP_LOG.en.md](TIMESTAMP_LOG.en.md)            |
| 表单扩展接口   | [form-integration.md](docs/form-integration.md) | [form-integration.en.md](docs/form-integration.en.md) |
| 备课、课堂流程与回滚 | [unified-workspace.md](docs/unified-workspace.md) | [unified-workspace.en.md](docs/unified-workspace.en.md) |

## 启动和试用

需要 Node.js 24 或更高版本。

```bash
git clone https://github.com/bd4rex/tongpin-classroom-feedback.git
cd tongpin-classroom-feedback
npm ci
npm run build
npm start
```

教师入口：`http://127.0.0.1:3210`；学生入口：`http://服务器IP:3210/join`。macOS 也可双击 `启动同频.command`。

1. 教师首次打开时设置一个管理密码，之后登录工作台。
2. 选择课堂模板，或创建空白课堂后点“快速备课”：套用反馈模板、打开备课包，或粘贴 AI 整理的任务 JSON，校验预览后保存草稿。在“采集与展示”勾选本节课需要的信息。
3. 预览学生看到的题目后分享课堂码或二维码。学生填写一次本节课要求的信息，后续回答自动关联。
4. 在统一工作台按组发布，直接查看反馈、学生提问和数据统计。学生自行选择组内顺序，草稿可恢复，完成后继续已开放的任务。
5. 结束课堂后回看或导出；可以只导出当前题的反馈，或保存任务组为备课包。复用不带入学生及历史回答。

服务器默认监听 `0.0.0.0:3210`。本机生成二维码时会尝试选取局域网地址；多网卡、VPN 或跨校网络请在 `.env` 中明确设置 `PUBLIC_URL`，并保证学生能访问该地址。复制 `.env.example` 为 `.env` 后，使用 `node --env-file=.env server/index.js` 启动。

服务端不再比较浏览器 `Origin` 与服务端 `Host`，因此用 `localhost`、`127.0.0.1`、局域网 IP 或反向代理入口访问时不会因为地址表现不同而拦截设置密码、加入课堂或提交反馈。写请求仍要求 JSON；教师接口还要求教师会话，学生接口还要求学生 Cookie；Cookie 使用 `HttpOnly` 和 `SameSite=Strict`。

## 已实现

- 内置七年级《无处不在的人工智能》：6 个教学环节、12 项任务，依据 PPT 与 40 分钟逐字稿改编。新建课堂时选择同名预置活动，按组下发，无需另外导入。详见[课程内容与使用说明](docs/lessons/ai-everywhere.md)（[English](docs/lessons/ai-everywhere.en.md)）。
- 本课只用选择、填空、AI 问答。填空支持 1–6 个独立输入，保存草稿、按题导出及教师参考答案；参考答案在公布后向学生开放，教师讲解提示始终仅教师可见。填空由教师讲评，不自动判分。

- 统一课堂工作台：任务编辑、学生视角预览、采集控制、反馈与统计集中在一个页面；可切回原布局。
- 快速备课模板、AI 备课说明、任务包校验预览和整组草稿导入；导出备课包跨课复用，保留任务说明与参考答案，不含学生资料。
- 单题完整 CSV 导出、`npm run backup` 一致性快照与已验证的旧版代码兼容；详见[备课与回滚](docs/unified-workspace.md)。

- 一节当前课堂，以及结束后的历史记录；无学生管理、招生、学籍、作业或成绩系统。
- 单选、多选、判断、投票、理解度、开放回答、填空、离堂反馈、AI 问答（原 AI 探究）。
- 任务组创建与编辑、草稿任务移组；按组发布、暂停、继续、限时结束和手动结束。单题结果可分别显示／隐藏。
- 同一课堂可以同时开放多个任务组；发布后续组不会关闭前组，也不会把学生强制切换到新题。
- 按组查看已完成／进行中／未开始，支持按学生现场填写的学校、班级汇总。
- 学生自由切换任务，未提交的答案和 AI 提问草稿暂存在当前浏览器标签页，关闭标签页后不保证保留。
- SSE 实时通知和断线重连；实时连接异常时自动轮询。
- 状态请求期间收到的新通知会合并为后续补刷，同一连接不会并发拉取或丢弃最后一次更新。
- 提交数、提交率、答案分布、参考答案正确率、在线参与端、按自填学校／班级汇总。
- 学生向教师提问；其他学生看不到原文。
- CSV 回答导出，JSON 完整记录导出（活动、回答、问题、分析、AI 互动）。
- SQLite 持久化；浏览器刷新或服务器重启后可恢复已提交回答。
- 教师按课堂配置城市、学校、班级、学号、姓名和昵称的显示／必填；城市与学校可设置联动选项。
- 教师数据统计、独立统计大屏、可选的学生统计页；参与信息明细及 CSV 导出仅教师可用。
- 开放回答的本地关键词词云，按题公布；支持排除词，无须调用 AI。

## 信息采集与统计大屏

创建课堂后打开 **“采集与展示”**。六项信息分别勾选“显示”和“必填”；不需要的项目不出现在加入页。城市学校名单每行填写 `城市｜学校`，最多 200 行，学校选项随城市筛选；不维护名单时由学生自填，无须提前知道任何学生。

关闭学校采集后，已有学校名单不再约束城市，学生可选择江苏省全部 13 个城市；重新开启学校后恢复名单联动。自动生成的参与编号仅用于显示，不计为学生填写的昵称，也不能满足后来开启的昵称必填。姓名／昵称编辑会回填学生实际填写的值。

学生填写一次后，回答、任务进度和提问关联到当前参与端。刷新页面或重启服务不要求重填；浏览器心跳会续期当前会话 Cookie，有效期为最近一次续期后的 24 小时。清除 Cookie、换设备或换浏览器会成为新参与端，信息是自报的，不作身份核验。学生可通过“我的参与信息”修改，已有关联记录显示更新后的信息。新增必填项时，已加入学生需补填后继续提交；关闭某项会停止新采集，但不会删除历史信息。复用课堂会复制设置，不复制参与信息或回答。

教师点击 **“数据统计”** 查看人数、在线端、城市／学校分布、组进度和每题反馈，展开“参与信息记录”查看或导出明细。点击 **“统计大屏”** 可打开适合投影的独立页面并全屏显示，需要教师登录。勾选“向学生开放课堂统计”后，学生页增加“课堂统计”入口。

大屏和学生端仅展示汇总，不返回姓名、学号、参与端明细或原始回答。选择题分布和文字词云随每题 **“展示本题结果”** 开放；教师可先预览。词云覆盖开放回答、离堂收获／疑问、AI 探究的最终反思，按提及该词的回答数统计，同一回答重复出现只计一次。每题最多使用最近 300 份、24,000 个字符，显示前 50 个词并标注是否抽样。已采集的姓名、学号、昵称及常见联系方式会被过滤，教师也可添加排除词；自由文本中的其他个人信息仍需教师预览核对。词频只反映提及情况，不代表正确率。

本版已参考 QuickForm 的任务采集流程，但尚未接入其在线服务或兼容其 API。稳定字段 ID、服务端校验和 `schemaVersion: 2` 完整 JSON 导出仍作为后续外部适配的基础，见[表单扩展接口](docs/form-integration.md)。

## 任务组如何使用

例如一节数学课分为“探索与练习”和“回顾与反思”两组。第一组可以同时包含两道选择题、一项开放任务，学生不必等待所有人完成同一道题。教师根据总体进度发布下一组，第一组默认继续开放；需要全体停下讨论时，教师可暂停相应任务组，或统一结束课堂。

“已完成”指该参与端提交了组内全部任务；“进行中”指打开过组内任务或已提交部分回答；“未开始”指尚无打开记录和回答。它表示页面参与情况，不推断真实学习状态。城市／学校／班级按教师设置现场采集，未填写的端归入“未填写”。

为了保持完成率的含义稳定，已发布组不再增加、删除或移动任务；临时补充练习可以新建一组。草稿组可自由编辑，默认由老师手动结束。升级前的逐题课堂会将每道原活动保留为独立组，不会意外公开旧草稿。

每个参与端每项活动提交一次；相同请求重试不会增加统计。以提交端为单位计算正确率和占比，**不把一个小组或一个班的回答折算为个人成绩**。更换浏览器、清除 Cookie 或使用新设备会成为新的匿名参与端，本版不解决身份核验或跨设备去重。

## AI 互动怎么开展

首版提供两个路径：

**学生探究**：教师添加“AI 探究”活动，写明任务及给模型的指令，设置每端最多 1–5 次提问。任务随所属任务组开放，时间由任务组统一控制。学生在任务范围内提问，阅读回答后提交评价或反思。每次为独立请求，需要在问题中补充上下文；不是无限多轮聊天。指令约束有助于维持教学主题，但不保证模型永远遵循主题。

**教师分析**：对于开放回答、离堂反馈和 AI 探究反思，教师主动点击“AI 辅助分析”。最多取 80 份、16,000 字样本，生成主要观点、需要核实的误解和后续追问。界面标明样本量与后续新增回答。采集的身份字段不会附加到模型请求；回答文本本身由学生填写，仍可能包含个人信息。

在“AI 设置”填入 OpenAI-compatible API 地址、模型名称和密钥。支持无密钥的本地模型；密钥保存在服务端 `data/model-config.json`，文件权限为 `0600`，不回显到浏览器。更换地址时需要重新填写或清除旧密钥，避免误发给其他服务。

保存设置不调用模型。点击“测试连接”、发布任务后学生提问、点击分析按钮，才会调用服务。自动化测试和本机容量验证使用模拟模型。首次部署后需要自行配置模型，并进行真实模型验证；现有测试不能代表真实模型答案质量或容量。

## 并发控制

| 普通反馈                                  | AI 请求                                      |
| ----------------------------------------- | -------------------------------------------- |
| 提交直接写入数据库，不调用模型            | 先入有界队列，再调用模型                     |
| 教师通知每 350ms 合并一次                 | 默认最多 4 个处理中、100 个等待              |
| 提交只通知教师，不向所有学生广播          | 每端同时最多一条请求                         |
| 只有发布、暂停等课堂控制通知全体学生      | 每项任务每端 1–5 次，服务端校验              |
| 汇总缓存 800ms；常规只返回最近 100 份回答 | 单次提问最多 1500 字，输出默认最多 600 Token |
| 学生每 25 秒心跳，75 秒内视为在线         | 单请求超时 25 秒，无自动付费重试             |

统计接口缓存 2 秒，教师／大屏每 3 秒读取一次；学生仅打开统计页时每 6–7.5 秒错峰读取。每次提交不触发全体学生刷新统计。词云样本有界，普通统计不占模型队列。

队列满时返回可重试提示，未接收的请求不扣次数。已接收的尝试计入额度，包括失败或取消的请求。暂停、手动结束任务组或结束课堂时，立即取消对应尚未派发的请求并释放等待名额；恢复任务组不会重新派发已取消请求，其他任务组的队列不受影响。已经发往模型的请求允许完成并保存结果。重启后，中断的请求会标记失败，不自动再次调用模型。

模型配置的同时处理数可设为 1–16，队列上限 1–200。教师分析和连接测试不会与学生任务同时争用队列；有未完成学生请求时提示稍后重试。一次分析／连接测试期间，学生新请求提示稍后重试。

容量应分别看“接入并提交反馈”和“同时使用模型”。本机 3000 端测试成功不表示能让 3000 个模型请求同时执行。正式试课前，应根据模型服务配额与实测延迟设置并发，可分批开放探究或采用小组终端参与。

## 校内服务器部署

直接运行上述 Node 命令即可。单实例运行，完整备份 `data/`。如果在线备份，请使用 SQLite Backup API；最简单的备份方式是先停止服务再复制整个目录，包括 WAL 文件。

也提供 Docker 配置：

```bash
docker compose up -d --build
```

Docker 的数据位于持久卷 `tongpin-data`。按实际情况设置 `PUBLIC_URL`。如使用 Nginx 反向代理，参考 `deploy/nginx.conf.example`；SSE 必须关闭代理缓冲。HTTPS 后设置 `COOKIE_SECURE=true`。

这是单服务器、单 Node 进程的 MVP。AI 调度器和通知订阅保存在进程内，SQLite 存储课堂记录。**不可直接启动多个实例共享数据库来扩容**；跨实例需要引入共享队列、发布订阅和共享数据库，并重新验证。尚未做公网部署、正式服务器实测或真实模型容量测试。

其他仍保留的边界是：教师首次设置密码且密码至少 8 个字符；登录和 AI 请求有频率／队列限制；接口有 JSON 和请求体大小校验；未登录不能操作教师接口，未加入课堂不能提交学生反馈；一节课只允许一个当前课堂；课堂结束后不能继续写入。它们分别保护鉴权、服务稳定性和课堂数据边界，不会要求学生预置账号。

## 验证和代码入口

```bash
npm test
npm run build
npm run test:capacity
```

测试使用独立临时数据库，不写入正式 `data/`，容量测试不访问真实模型。容量结果输出到 `output/capacity-results.json`；当前验收记录见 [TEST_REPORT.md](TEST_REPORT.md)。

| 路径                                                                | 用途                                      |
| ------------------------------------------------------------------- | ----------------------------------------- |
| `src/App.jsx`、`src/TaskGroups.jsx`、`src/styles.css`               | 教师和学生页面、任务组及自主进度          |
| `server/app.js`                                                     | HTTP API、鉴权、SSE、课堂控制             |
| `server/store.js`                                                   | SQLite 数据结构、统计、输入校验           |
| `server/collection.js`、`src/Collection.jsx`                        | 采集字段配置、学生表单、教师参与明细      |
| `server/dashboard.js`、`server/word-cloud.js`、`src/Statistics.jsx` | 汇总、词云、统计页和大屏                  |
| `server/ai.js`                                                      | 模型配置、服务端调用、有界队列和分析      |
| `server/templates.js`                                               | 可修改的多学科示例活动                    |
| `test/app.test.js`                                                  | 课堂闭环、权限、统计、AI 限流和持久化测试 |
| `scripts/capacity.js`                                               | 本机 HTTP／SSE 并发和模拟模型队列测试     |

实施参考：用户提供的《江苏省乡村学校远程 AI 通识课实时反馈系统实施计划》。以用户后续明确的“通用、单教师单课堂、学生现场匿名加入、优先并发和模型互动、按组发布并由学生自主推进”为本版范围；未把原方案的省级治理、完整账号体系和全部远期功能纳入 MVP。
