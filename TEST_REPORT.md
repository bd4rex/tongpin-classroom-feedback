# MVP 验证记录

[English](TEST_REPORT.en.md)

最新验证：2026-09-19，版本 0.2.0。范围是本机信息采集、会话关联、教师／学生统计与大屏，以及多入口来源限制修复。本轮进一步将默认城市限制为江苏省内、身份改为姓名或昵称二选一，学生入口固定为个人参与。未部署校内正式服务器，未调用真实模型；本轮已通过 PR #2 合并到公开仓库 `main`。

## 功能验证

`npm run check` 通过：24 项集成测试、JavaScript 语法检查及前端生产构建通过。`npm audit --omit=dev` 报告 0 项已知漏洞。

来源兼容性回归：带不一致 `Origin` 的 JSON 写请求不再返回 403，而是进入正常的教师鉴权流程（未登录时返回 401）；非 JSON 写请求仍返回 415。主服务 `http://127.0.0.1:3210/api/health` 返回版本 `0.2.0`。

保留原有课堂、任务组、自主进度、提交幂等、不同参与方式、权限隔离、数据库恢复、CSV 防护和 AI 队列测试。本轮新增姓名或昵称二选一回归，合计 25 项测试，覆盖：

- 六项采集字段的显示与必填、城市学校合法组合、未启用字段不接收新值。
- 同会话重新加入不重复建档；学号前导零、回答关联、心跳续期和服务重启后恢复。
- 课堂中新增必填项后先补填再提交；关闭字段保留历史，修改信息不新增参与端。
- 教师预览、大屏与学生汇总的逐题公布控制；草稿和原始身份明细不泄露；关闭展示立即影响接口；不同城市同名学校分开统计。
- 教师参与记录分页与 CSV、JSON 导出关系、CSV 公式防护；复用课堂仅复制设置。
- 词云按回答去重计词、排除词和已采集标识过滤、样本与字符预算有界。
- 默认课堂开放江苏省 13 个城市；城市学校名单拒绝省外城市，姓名和昵称不能同时设为必填，学生入口仅提交个人参与方式。

## 浏览器验证

使用独立 `output/collection-browser-data` 数据目录及明确标注的测试姓名和学校，未向正式 `data/` 添加测试记录。

- 教师在界面创建科学课堂，启用城市、学校、班级、学号、姓名必填并关闭昵称；设置城市学校列表、学生统计及词云。
- 390px 学生加入页只显示启用字段；选择南京市后，学校下拉不出现苏州市的学校。
- 学生填写一次后加入、刷新，直接恢复课堂；“我的参与信息”保留学号 `000012`。
- 学生完成选择题和开放回答，教师查看两份自动关联的反馈、整组完成进度及参与信息记录。
- 教师公布前，学生统计只显示提交量；公布后自动出现词云。学生回答中刻意写入测试姓名及学号，词云已过滤这两个值。
- 教师统计页有身份明细；学生统计及投影页无参与明细、姓名或学号字段。学生页顶部仍显示其自己的名称。
- 教师统计大屏在新标签页打开；1920×1080 下本次课堂完整显示于一屏，进入／退出全屏正常。
- 教师页 1440px、教师及学生页 390px 无页面水平溢出；较宽表格在卡片内部横向滚动。截图已逐张检查。

截图在本机 `output/playwright/collection-*.png`。既有按组异步进度和 AI 浏览器闭环于 2026-09-12 验证；本轮新增规则已做服务端与生产构建回归，未重新生成浏览器截图，未把模拟输出当作真实模型效果。

## 采集、反馈与统计并发

原始记录：[capacity-collection-stats-2026-09-19.json](docs/validation/capacity-collection-stats-2026-09-19.json)。

环境：Apple M3，8 个逻辑 CPU，16 GiB 内存，macOS arm64，Node.js v24.14.1。服务端为独立 Node 进程与 SQLite WAL，压测客户端同机；保留每端真实 HTTP／SSE 连接，HTTP 连接池最多 200 个并行请求。耗时包含客户端排队。

每端采集六项信息，整组发布两道选择题和一道文字题。学生按三种顺序连续完成三项任务；公布文字结果后，各端并发读取学生统计，验证样本上限为 300 且采集标识不出现在词云。

| 参与端／SSE 连接 | 成功写入 | 失败 | 全部提交耗时 | 提交 P95 | 发布到状态读取完成 P95 | 统计读取 P95 |
| ---------------- | -------- | ---- | ------------ | -------- | ---------------------- | ------------ |
| 500              | 1500     | 0    | 332 ms       | 114.2 ms | 304.4 ms               | 64.7 ms      |
| 1000             | 3000     | 0    | 648 ms       | 228.3 ms | 233.3 ms               | 98.4 ms      |
| 3000             | 9000     | 0    | 1962 ms      | 671.6 ms | 507.4 ms               | 278.2 ms     |

统计请求各档均无失败，数据库回答数和整组完成人数符合预期。“状态读取完成”是 API 指标，不包含数千台设备的页面渲染。

3000 连接继续保持约 33 秒，6 轮共 18,000 次心跳及 18,000 次统计读取，失败为 0。RSS 采样为 428、429、446、448、438、437 MiB；全程观测服务进程峰值 448 MiB，不含客户端、浏览器或模型推理内存。

这是本机短时模拟负载，**不等于正式环境承诺 3000 名学生，也不是完整课时的稳定性验收**。没有覆盖校园网络、手机设备、代理、Wi-Fi、真实模型和目标服务器资源。

## AI 队列

独立模拟服务每条延迟 120ms；100 个请求集中到达，并发设为 4、等待容量 40。接收并完成 44 个，56 个得到 HTTP 429，观测上游并发最大 4。普通反馈与统计不调用模型。

原有测试继续验证单端单请求、幂等、次数限制、暂停后取消排队、失败／非法响应释放槽位和分析不附加身份字段。模拟测试不能用于估计真实模型吞吐、等待时间或费用。

## 本地升级与历史记录

现有 `data/classroom.sqlite` 升级前通过 SQLite Backup API 备份至忽略目录 `output/backups/`，完整性检查通过。新字段为兼容性添加，旧课堂沿用默认选填设置；本地 3210 端口启动 0.2.0。运行数据、模型配置、构建产物与截图不进入 Git。

2026-09-12 的 18 项测试及任务组容量基线保留于 [capacity-task-groups-2026-09-12.json](docs/validation/capacity-task-groups-2026-09-12.json)。仓库公开及 GitHub 发布记录见 [TIMESTAMP_LOG.md](TIMESTAMP_LOG.md)，此次功能更新已随 PR #1 进入 `main`。

## 正式试课仍需验证

1. 在实际服务器及网络重跑容量测试，并加入真实手机／平板。
2. 配置真实模型后限额试课，测量配额、延迟、费用和输出质量。
3. 完整课时保持、断网恢复、服务重启及备份恢复。
4. Docker／Nginx 部署路径的端口、SSE 缓冲、文件句柄和持久卷；本次未运行 Docker 镜像或修改现有服务器。
