# MVP 验证记录

[English](TEST_REPORT.en.md)

验证日期：2026-09-12。范围为本机可运行 MVP，未部署到校内正式服务器，未调用真实模型。

## 功能验证

`npm run check` 通过：18 项集成测试通过，JavaScript 语法检查与前端生产构建通过。`npm audit --omit=dev` 未报告已知漏洞。

覆盖单教师单当前课堂、学生现场匿名加入、不同参与方式统计、课堂间隔离、答案不提前泄露、重复提交幂等、整组限时／暂停／继续、跨组同时开放、任意顺序作答、学校分组进度、草稿移组、升级旧数据不暴露草稿、全部反馈题型、200 端集中提交、学生提问隔离、CSV 公式防护、完整导出、数据库重启恢复、AI 并发／队列／次数限制、模型故障和非法响应恢复、分析请求不附带身份字段。

## 浏览器验证

在独立 `output/browser-data` 数据目录验证，未写入正式 `data/`。

- 教师设置管理密码、创建数学模板课堂，一次发布“探索与练习”中的 3 项任务。
- 两个独立学生页面同时参与，快端按第 3、1、2 项顺序完成，慢端停留第一项；教师看到已完成 1 端、进行中 1 端。
- 发布“回顾与反思”后，慢端仍保留第一组题目和未提交选项，并能成功提交；快端可以直接进入下一组。
- 开放回答草稿在切换任务后保留；学生页面不提前显示后续草稿组。
- 教师暂停第一组后，学生端第一组未答任务不能提交，第二组仍能正常提交。
- 学生仅使用课堂码加入，昵称自动生成；手机端答题后教师统计自动更新。
- 通过设置页面接入本地模拟 HTTP 模型服务，连接测试成功。
- 旧版 AI 互动闭环已验证：学生提问收到模拟回复，提交反思后教师看到反馈并生成分析。任务组版本补充验证新建“AI 拓展”组、添加两项任务并一次发布；学生 AI 问题草稿切换后保留，提问得到明确标注的模拟回复，并成功提交反思。
- 桌面 1440px 与手机 390px 检查；教师页、学生答题页和 AI 互动页均无水平溢出。
- 浏览器联调使用显式标注的模拟回答，不作为真实模型答案质量验证。

截图保存在本机 `output/playwright/`。开发过程中重启测试服务曾产生临时连接错误；恢复后 SSE 自动重连，课堂和已提交回答保留。初次无学生会话时的 401 请求已调整为正常的未加入状态。

## 普通反馈并发

原始记录：[capacity-task-groups-2026-09-12.json](docs/validation/capacity-task-groups-2026-09-12.json)。该记录为任务组版本的重新验证；旧逐题基线仍保留在同目录。

环境：Apple M3，8 个逻辑 CPU，16 GiB 内存，macOS arm64，Node.js v24.14.1。服务端为独立 Node 进程、SQLite WAL；压测客户端与服务端位于同一台电脑。

每档先建立对应数量的真实 HTTP／SSE 连接，再一次发布包含 3 项任务的任务组、读取学生状态。所有客户端集中开始，各端按 3 种不同顺序连续完成整组；每端收到上一项提交响应后再提交下一项。HTTP 连接池最多 200 个请求并行；提交时仍保留全部 SSE 连接。耗时含客户端连接池等待。

| 参与端／SSE 连接 | 写入成功 | 失败 | 全部提交耗时 | 提交 P95 | 发布到学生状态读取完成 P95 |
| ---------------- | -------- | ---- | ------------ | -------- | -------------------------- |
| 500              | 1500     | 0    | 306 ms       | 107.9 ms | 273.5 ms                   |
| 1000             | 3000     | 0    | 525 ms       | 176.4 ms | 286.9 ms                   |
| 3000             | 9000     | 0    | 1692 ms      | 571.4 ms | 509 ms                     |

所有档位的数据库回答数、单题提交数、整组完成人数与参考答案正确率均与预期一致。上表的“状态读取完成”是 API 网络链路指标，不包含 3000 台真实设备的页面渲染。

3000 个连接随后进行了约 31 秒短时保持检查，6 轮、共 18,000 次心跳请求无失败。服务进程 RSS 采样为 394、382、394、410、411、411 MiB；全程观测峰值约 411 MiB。它不包含客户端和浏览器内存，也不包含真实模型推理资源。

这是短时本机测试，**不是整节课的稳定性验收，也不等于承诺正式环境支持 3000 名真实学生**。结果不能直接换算为普通 x86 服务器配置，未包含校园出口、Wi-Fi、VPN、反向代理、手机浏览器和真实模型服务。

## AI 队列并发

独立模拟模型每次延迟 120ms，仅用于检验调度，完全不访问模型厂商。

| 项目                     | 结果 |
| ------------------------ | ---- |
| 集中到达请求             | 100  |
| 设置的模型并发           | 4    |
| 设置的排队容量           | 40   |
| 接收并完成               | 44   |
| 以 HTTP 429 提示稍后重试 | 56   |
| 实测最大上游并发         | 4    |

另有测试验证单端同时一请求、重复请求幂等、次数限制、暂停后取消未派发请求、模型连接失败／非法响应后释放槽位。此结果不能用来估计真实模型每分钟吞吐、费用或等待时长。

## 本次 GitHub 发布检查

2026-09-12 11:05（UTC+08:00）重新执行 `npm run check`：18 项测试通过，语法检查与生产构建通过。`npm audit --omit=dev` 报告 0 项已知漏洞。

上传范围包含应用代码、测试、脚本、部署示例、双语文档和现有模拟容量记录；不包含 `data/`、`output/`、`node_modules/`、`dist/`、`.playwright-cli/` 或真实 `.env` 文件。待发布文件中的固定密码与密钥均为测试示例。

上文浏览器和容量数据保留自同日已有验证记录，本次发布准备未重新进行浏览器操作或容量测试。GitHub 发布不代表服务器部署或真实模型验收。

## 仍需在正式环境验证

1. 使用拟部署服务器和实际网络，重跑 `npm run test:capacity`，补充真实手机／平板接入。
2. 用实际模型配置进行限额试课，测量响应延迟、供应商并发与请求速率限制、输出 Token 和费用，再调整队列配置。
3. 做覆盖完整课时的保持、断网重连、服务重启与备份恢复检查。
4. 如使用 Docker 或 Nginx，验证该部署路径的端口、SSE 缓冲、文件句柄和持久卷。本轮未运行 Docker 镜像，也未修改任何现有服务器。
